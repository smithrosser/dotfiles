#
# looks.conf
# Look and feel configuration
#

# Wait for hyprpaper to launch, and set wallpaper
exec-once = sleep 1; hyprctl hyprpaper wallpaper ",~/Pictures/arch_nz.png"

# Important environment variables for cursor size
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    layout = dwindle # Use traditional i3-like tiling
    allow_tearing = true # Disable vsync

    # Window border settings
    gaps_in = 5
    gaps_out = 5
    border_size = 3
    col.active_border = rgba(00ff99ee) 
    col.inactive_border = rgba(595959aa)
    resize_on_border = false
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 5
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 0.95
    inactive_opacity = 0.85

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 5
        passes = 2
        vibrancy = 0.1
    }
}

bezier = snappy, 0.05, 0.9, 0.1, 1.05
animations {
    enabled = yes
    animation = windows, 1, 2, snappy
    animation = windowsOut, 1, 3, default, popin 80%
    animation = fade, 1, 3, default
    animation = workspaces, 1, 2, default, slide
}
